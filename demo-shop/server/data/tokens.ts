/**
 * Pre-generated Cashu tokens for the free claim demo feature.
 * 
 * TO POPULATE THIS ARRAY:
 * 1. Open the Nutpay extension and ensure you have some sats
 * 2. Go to "Send" and create small ecash tokens (1-2 sats each)
 * 3. Copy the generated cashuA... or cashuB... strings
 * 4. Paste them into this array
 * 
 * Each token should be a small amount (1-2 sats) for demo purposes.
 * The extension's ecash-scanner detects tokens matching:
 *   /\b(cashu[AB][A-Za-z0-9_\-=+/]{20,})\b/g
 */
export const freeTokens: string[] = [
  // Add pre-generated tokens here, e.g.:
  // 'cashuAeyJ0b2tlbiI6W3sicHJvb...',
];
